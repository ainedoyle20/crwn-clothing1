export type AdditionalInformation = {
    displayName?: string;
}

export type ObjectToAdd = {
    title: string;
}

export type UserData = {
    createdAt: Date;
    displayName: string;
    email: string;
}
